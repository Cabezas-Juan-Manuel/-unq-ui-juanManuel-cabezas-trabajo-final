export class Ship {
    constructor(length) {
      this.length = length;
      this.coordinates = []; // Array para almacenar las coordenadas del barco
    }

    sink(){
        this.sink = true
    }

    takeAHit(){
            
    }
    
  }